{
  "dashboard": {
    "title": "Animal Picture Service Dashboard",
    "panels": [
      {
        "title": "Total Requests",
        "type": "graph",
        "targets": [
          {
            "expr": "sum(rate(app_requests_total[5m]))"
          }
        ]
      }
    ]
  }
}
